class VertexBuffer {
  int mRenderedId = 0; // this is the vertex buffer id basically.
  dynamic data; // the buffer data: [].
  dynamic size; // the buffer size: int.

  VertexBuffer(this.data, this.size);
}
